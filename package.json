{
  "name": "json-gen",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "run": "node src/generate_metadata.js",
    "upload": "node src/upload_metadata.js "
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "fs": "^0.0.1-security",
    "ipfs-client": "^0.8.1"
  }
}
